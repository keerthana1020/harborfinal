apiVersion: v1
kind: PersistentVolume
metadata:
  name: jobservice-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: gp3-csi
  awsElasticBlockStore:
    volumeID: <EBS_VOLUME_ID_JOBSERVICE>
    fsType: ext4
