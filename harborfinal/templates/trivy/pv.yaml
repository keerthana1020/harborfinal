apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trivy-pv
  namespace: harborfinal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp3-csi
