apiVersion: v1
kind: PersistentVolume
metadata:
  name: trivy-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: gp3-csi
  awsElasticBlockStore:
    volumeID: <EBS_VOLUME_ID_TRIVY>
    fsType: ext4
